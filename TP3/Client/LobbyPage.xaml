<Page x:Class="IFT585_TP3.Client.LobbyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"       
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LobbyPage" Background="#FF909090">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListBox
            Name="GroupsListBox"
            Grid.Column="0"
            Grid.Row="0"
            Background="Transparent" Grid.ColumnSpan="2" Margin="0,0,0,0.2">
            <ListBoxItem Padding="10" Content="Test" Selector.IsSelected="True"/>
            <ListBoxItem Padding="10" Content="Test2" />
        </ListBox>
        <Button 
            Grid.Column="0"
            Grid.Row="1"
            Content="Add new"
            Padding="10"/>
    </Grid>
</Page>
